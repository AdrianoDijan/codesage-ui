services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8090:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
